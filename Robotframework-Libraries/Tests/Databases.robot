*** Settings ***
# it is an external library to install use https://github.com/franz-see/Robotframework-Database-Library
# it also depends on other libarry depending which BD you want to interact with so you need extra install
# MySQL = pip install pymysql
# SQL = pip install pymssql

Library    Databases
